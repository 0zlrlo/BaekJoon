//N개의 수가 주어졌을 때, 이를 오름차순으로 정렬
#include <iostream>
#include <algorithm>
using namespace std;
int main(){
    int N;
    int num[10000];
    for(int i=0;i<N;i++){
        num[i];
    }
    sort(num,num+)
}